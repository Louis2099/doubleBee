What each panel shows:

(a) thrust vs PWM — your 11 bench points and the degree-4 polynomial that sim uses. The dotted line is W/2 per propeller, so where the curve crosses it, two props equal body weight (~1570 µs). The dashed line is your real hover point, 1335 µs.

(b) power vs PWM — same, for electrical watts. Note it's steeper than thrust: doubling thrust costs far more than double the power.

(c) residuals — how far the fits sit from the measurements, as % of reading. Thrust is essentially exact (worst 0.089%); power is worse at the bottom end (−3.65% at 1100 µs) where absolute values are small.

(d) propeller efficiency, N/W — the only panel that makes an argument. Efficiency falls 66% from 1100 to 2000 µs. That is the physical reason a policy that modulates thrust beats one holding constant hover: every watt spent above the minimum buys progressively less thrust. Your entire energy claim rests on this curve, and right now the paper asserts it rather than showing it.

Does it deserve a place? Panel (d) yes. Panels (a)–(c) not as they stand — and here's the honest problem.

You fit 5 coefficients to 11 points and then report the residuals of that same fit. That's calibration error, not validation error. Any reviewer who notices will say "of course a quartic fits 11 points," and R4 asked specifically for "calibration and validation of the energy/power model." Right now I can only give you the calibration half.

Since the robot is next to you, two things turn this into real validation:

1. Six interleaved bench points — 30 minutes. Measure thrust and power at 1050, 1150, 1250, 1350, 1450, 1550 µs — the midpoints between your existing samples. Those are held-out: the fit has never seen them. Then panel (c) becomes prediction error on unseen data, which is what validation means. Add them to PWM2TE.csv in a split column and I'll update the script to plot fit vs held-out separately.

2. The stronger one, and it's nearly free — end-to-end energy validation. You already record accumulated ∫V·I dt from the flight controller (356 J for the RL trial, 571 J for Decouple 1 in the IROS submission). So for every trial you have both:

predicted energy: integrate the power model over the commanded PWM trace
measured energy: the FCU's ∫V·I dt
Plot predicted vs measured, one point per trial, against a y = x line. That validates the model on the actual task, at real operating points, including battery sag and ESC losses the bench never saw. It's a much better figure than a bench re-measurement, and it directly answers R4.

If they diverge, that's also publishable — it quantifies how much your bench model understates true consumption, which is exactly the kind of detail the review said was missing.

Do #2 with data you already have. Point me at one FCU log alongside its hw_*.csv and I'll write the comparison.

---

The thrust and power models are degree-4 fits to 11 bench measurements spanning 1000–2000 µs, with calibration RMSE 0.0039 N and 1.65 W (0.013% and 0.148% of range). Propeller efficiency falls 66% across this range, from 0.087 N/W at 1100 µs to 0.029 N/W at 2000 µs; holding at T/W 0.55 costs 41% of the power required to hover.

---

The paper's title term is a rounding error in the reward. Your instinct is correct, and you need to know this before you write, not after a reviewer finds it.

Run the ablation — but as a sweep, not a binary
If you run only w_E = 0 vs 0.25, I'd predict almost no difference, and then you've spent 5 hours learning that 0.25 does nothing. That's a null result about a weight, not about energy-awareness.

The penalty is exp(-E/20) - 1, currently operating at f = -0.248/step — that's in the responsive part of the curve, not saturated. So the term is sensitive to energy; it's just weighted into irrelevance. Sweep until it bites:


w_E ∈ {0.0, 0.25, 1.0, 4.0}
At 4.0 it contributes ≈ −0.21, comparable to penalize_rotation — that's where it can actually compete. Four runs, parallelizable, ~2.5 h each.

That converts an ablation into a contribution. "We penalize energy" is a claim; "here is the measured success-vs-energy Pareto front, and here is where we chose to operate" is a result. It also answers R4's reward-sensitivity complaint directly.

---

The stronger energy experiment is already registered

Isaac-Velocity-HybridStair-DoubleBee-ConstThrust-v1-ppo
Same policy, same task, thrust pinned instead of modulated. If the modulating policy uses less energy at equal success, modulation earns its keep — and that's the non-trivial comparison. R1's objection was that "4× better than propeller-only" is trivially true (flying the whole way is obviously expensive). Constant-thrust hybrid is the baseline that isn't a strawman.

This one is worth more than the sweep, because it isolates modulation from reward weighting.

On contributions — the honest read
Your problem isn't a missing idea. It's missing evidence, and you just fixed the biggest piece of it.

Go back through the reviews: C1 baselines, C2 trial count, C3 "unable to maintain stable balance," C5 no validation. Not one reviewer questioned the novelty. R4 watched your video and saw a robot that couldn't balance — that's what sank it. Two days ago that robot climbed 11 cm over two risers and drove 1.87 m. That single fact moves you further than any reframing.

What you can defensibly claim:

A learned policy coordinating wheels and propellers on a propeller-assisted balancing robot, where the platform's own prior work (Cao et al.) used a hand-designed decoupled controller. Learned vs hand-designed on the same machine — and you have that baseline implemented.
A validated actuator/energy model — thrust RMSE 0.0039 N (0.013% of range), power RMSE 1.65 W, against bench data.
A measured energy trade-off — if the sweep produces a front.